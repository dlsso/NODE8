extends layout

block content
	.container
		h1 Applicants
		ul#applications.list-unstyled
			for user in data
				li 
					p.name
						a(href='/show/#{user._id}') #{user.name}
					button.delete Delete